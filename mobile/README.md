# Mobile App

Flutter 客户端，直连 Alpaca API。完整说明见仓库根目录 [README](../README.md)。

```bash
flutter pub get
flutter build apk --release    # Android
flutter build windows --release # Windows
```

Windows 打包脚本：`build_android.bat`
